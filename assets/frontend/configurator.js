/* ConfigKit storefront configurator. Filled in CHUNK 2+. */
( function () {
	'use strict';
	var mount = document.getElementById( 'configkit-configurator' );
	if ( ! mount ) return;
	// CHUNK 1 stub: just remove the loading skeleton so the page is
	// visibly empty until the full app lands.
	mount.replaceChildren();
} )();
